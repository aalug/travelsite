SECRET_KEY=your django secret key

# DB configuration
# all db values compare with docker-compose.yml file
DB_HOST=
DB_NAME=
DB_USER=
DB_PASSWORD=
PORT=5432

# email configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your email address
EMAIL_HOST_PASSWORD=your password (generated by gmail)
DEFAULT_FROM_EMAIL=name that should appear when receiving email from this website

# Google API
API_KEY=your api key generated for location search feature to work